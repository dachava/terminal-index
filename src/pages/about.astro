---
import BaseLayout from '../layouts/BaseLayout.astro';
import RetroWindow from '../components/RetroWindow.astro';
import AvatarFrame from '../components/AvatarFrame.astro';
import '../styles/about.css';

const skills = {
  Cloud:  ['AWS', 'Azure', 'GCP'],
  DevOps:   ['Terraform', 'Ansible', 'Git', 'Jenkins', 'Azure DevOps', 'ArgoCD'],
  Scripting:    ['Python', 'Bash', 'Powershell'],
  Platforms:      ['Linux', 'Unix', 'Win Server 20XX', 'Docker', 'Kubernetes'],
  Security:    ['SAST/DAST', 'API-driven security controls', 'SAML/SSO'],
  Databases:    ['MSSQL', 'MySQL', 'PostgreSQL', 'Oracle DB', 'MongoDB'],
};

const timeline = [
  {
    year: '2020 - 2025',
    title: 'Senior Application Security Platform Engineer',
    org: 'OpenText',
    desc: 'Reduced critical vulnerabilities by 30% by deploying and operating SAST/DAST tooling at scale across hybrid AWS/Azure environments, leading risk-based triage of findings across application code and dependencies',
  },
  {
    year: '2016 - 2020',
    title: 'Senior Infrastructure Reliability Engineer',
    org: 'Micro Focus',
    desc: 'Accelerated incident recovery by 20% by commanding real-time response across OS, storage and recovery layers, coordinating hardware, platform and operations teams to restore production services within SLA targets',
  },
];

const education = [
  {
    year: 'Feb 2026',
    title: 'AWS Certified Cloud Practitioner (CLF-C02)',
    org: 'Amazon Web Services',
    desc: '',
  },
  {
    year: 'Oct 2025',
    title: 'Azure Administrator Associate (AZ-104)',
    org: 'Microsoft Certified',
    desc: '',
  },
  {
    year: 'Jun 2024',
    title: 'Bachelor of Engineering in Computer Science',
    org: 'Universidad Fidélitas, San José',
    desc: '',
  },
];

const contacts = [
  { host: 'github.com',    href: 'https://github.com/dachava',       reply: '@dachava',          ms: 4 },
  { host: 'linkedin.com',  href: 'https://linkedin.com/in/diegochavab', reply: 'in/diegochavab', ms: 12 },
  { host: 'dachava.dev',   href: 'mailto:hello@dachava.dev',         reply: 'hello@dachava.dev', ms: 1  },
  { host: 'rss',           href: '/rss.xml',                         reply: '/rss.xml',          ms: 0  },
];
---

<BaseLayout
  title="About"
  description="Software engineer, web developer, digital packrat. This is the about page."
>
  <div class="about-page container">
    <header class="page-header">
      <div class="page-header-prefix" aria-hidden="true">$ ./about.sh</div>
      <h1 class="page-title">About</h1>
    </header>

    <!-- Profile — character sheet layout -->
    <section class="profile-section" aria-labelledby="profile-heading">

      <!-- Top row: avatar + stat card -->
      <div class="profile-top">

        <!-- Avatar (standalone, no window chrome) -->
        <div class="profile-avatar">
          <AvatarFrame src="/images/about.png" lightSrc="/images/about-light.png" alt="Diego Chavarría" size={180} />
        </div>

        <!-- Stats — raw terminal output, no window chrome -->
        <div class="stat-block">
          <h2 id="profile-heading" class="stat-name">
            <span class="glow-cyan">Diego</span> <span class="glow-magenta">Chavarría</span>
          </h2>
          <p class="stat-handle">@Prosperity&Beauty</p>
          <div class="stat-divider" aria-hidden="true"></div>
          <dl class="stats">
            <div class="stat-row">
              <dt class="stat-key">class</dt>
              <dd class="stat-val">Software Engineer</dd>
            </div>
            <div class="stat-row">
              <dt class="stat-key">location</dt>
              <dd class="stat-val">San José, CRC</dd>
            </div>
            <div class="stat-row">
              <dt class="stat-key">focus</dt>
              <dd class="stat-val">cloud · systems · security</dd>
            </div>
            <div class="stat-row">
              <dt class="stat-key">status</dt>
              <dd class="stat-val stat-status"><span class="status-pip" aria-hidden="true"></span>online</dd>
            </div>
          </dl>
        </div>
      </div>

      <!-- Bio panel -->
      <RetroWindow title="bio.md" class="bio-card">
        <div class="profile-text" id="profile-heading">
          <p>
            I'm a cloud and infra engineer 9+ years in. Designed Disaster Recovery solutions and commanded AppSec efforts.
            Sometimes I have to put some fires out, hey you can't win them all.
          </p>
          <p>
            I've worked across platform engineering and security, which basically means I've seen what happens when you ignore both. 
            Lately I'm mostly focused on cloud architecture, K8S, and automating anything that's worth it.
          </p>
          
        </div>
      </RetroWindow>

    </section>

    <!-- Timeline -->
    <section class="timeline-section" aria-labelledby="timeline-heading">
      <div class="section-heading">
        <h2 id="timeline-heading">experience</h2>
      </div>

      <ol class="timeline" role="list">
        {timeline.map((item, i) => (
          <li class="timeline-item">
            <div class="timeline-year">{item.year}</div>
            <div class="timeline-connector" aria-hidden="true">
              <span class="timeline-dot"></span>
              {i < timeline.length - 1 && <span class="timeline-line"></span>}
            </div>
            <div class="timeline-content">
              <h3 class="timeline-title">{item.title}</h3>
              <p class="timeline-org">{item.org}</p>
              <p class="timeline-desc">{item.desc}</p>
            </div>
          </li>
        ))}
      </ol>
    </section>

    <!-- Skills inventory -->
    <section class="skills-section" aria-labelledby="skills-heading">
      <div class="skills-prompt" aria-hidden="true">
        <span class="prompt-user">621</span><span class="prompt-at">@</span><span class="prompt-host">dachava.dev</span><span class="prompt-sep">:</span><span class="prompt-path">~</span><span class="prompt-dollar">$</span>
        <span class="prompt-cmd">tree ~/skills</span>
      </div>

      <div class="skills-tree" role="list" aria-label="Skills and tools">
        <div class="tree-root" aria-hidden="true">skills/</div>
        {Object.entries(skills).map(([category, items], ci) => {
          const isLast = ci === Object.entries(skills).length - 1;
          return (
            <div class="tree-category" role="listitem">
              <span class="tree-branch" aria-hidden="true">{isLast ? '└──' : '├──'}</span>
              <span class="tree-cat-name">{category.toLowerCase()}/</span>
              <div class="tree-chips">
                {items.map(skill => (
                  <span class="skill-chip">{skill}</span>
                ))}
              </div>
            </div>
          );
        })}
      </div>
    </section>

    <!-- Education -->
    <section class="timeline-section" aria-labelledby="education-heading">
      <div class="section-heading">
        <h2 id="education-heading">education</h2>
      </div>

      <ol class="timeline" role="list">
        {education.map((item, i) => (
          <li class="timeline-item">
            <div class="timeline-year">{item.year}</div>
            <div class="timeline-connector" aria-hidden="true">
              <span class="timeline-dot"></span>
              {i < education.length - 1 && <span class="timeline-line"></span>}
            </div>
            <div class="timeline-content">
              <h3 class="timeline-title">{item.title}</h3>
              <p class="timeline-org">{item.org}</p>
              {item.desc && <p class="timeline-desc">{item.desc}</p>}
            </div>
          </li>
        ))}
      </ol>
    </section>

    <!-- Contact -->
    <section class="contact-section" aria-labelledby="contact-heading">
      <h2 id="contact-heading" class="sr-only">Contact</h2>

      <div class="ping-prompt" aria-hidden="true">
        <span class="prompt-user">621</span><span class="prompt-at">@</span><span class="prompt-host">dachava.dev</span><span class="prompt-sep">:</span><span class="prompt-path">~</span><span class="prompt-dollar">$</span>
        <span class="prompt-cmd">ping -c 1 {contacts.map(c => c.host).join(' ')}</span>
      </div>

      <ul class="ping-list">
        {contacts.map(({ host, href, reply, ms }, i) => (
          <li class="ping-row" style={`animation-delay:${i * 120}ms`}>
            <a
              href={href}
              class="ping-link"
              aria-label={reply}
              {...href.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {}}
            >
              <span class="ping-seq" aria-hidden="true">64 bytes from</span>
              <span class="ping-host">{host}</span>
              <span class="ping-sep" aria-hidden="true">:</span>
              <span class="ping-reply">{reply}</span>
              <span class="ping-spacer" aria-hidden="true"></span>
              <span class="ping-time" aria-hidden="true">time={ms}ms</span>
              <span class="ping-open">open →</span>
            </a>
          </li>
        ))}
      </ul>
    </section>
  </div>
</BaseLayout>

